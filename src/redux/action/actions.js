const DoSomething =(payload)=>({
    type: 'TEMPLATE',
    payload: payload

})




const actions = {
    //put here actions functions to export like:
    DoSomething: DoSomething
}
export default actions